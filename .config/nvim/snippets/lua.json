{
  "use": {
    "prefix": "use",
    "body": [
      "  use({",
      "    \"$1\"",
      "    config = function()",
      "        $2",
      "    end,",
      "  })"
    ],
    "description": "Add a plugin"
  }
}
